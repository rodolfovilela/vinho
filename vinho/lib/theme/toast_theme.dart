import 'dart:ui';

class ToastTheme {
  static const Color successBg = Color.fromRGBO(232, 223, 214, 1); // mutedBackground
  static const Color successText = Color.fromRGBO(45, 24, 16, 1);  // primaryColor

  static const Color errorBg = Color.fromRGBO(107, 40, 52, 1); // primaryRed
  static const Color errorText = Color(0xFFFFFFFF);

  static const Color infoBg = Color.fromRGBO(248, 245, 240, 1); // lightBackground
  static const Color infoText = Color.fromRGBO(45, 24, 16, 1);

  static const Color border = Color.fromRGBO(109, 77, 58, 0.15); // semiTransparent
}